services:
  db:
    image: postgres:16-alpine
    container_name: dichvutracuu-db
    restart: unless-stopped
    environment:
      POSTGRES_USER: postgres
      POSTGRES_PASSWORD: postgres
      POSTGRES_DB: dichvutracuu
    ports:
      - "5432:5432"
    volumes:
      - postgres_data:/var/lib/postgresql/data
      - ./database/init.sql:/docker-entrypoint-initdb.d/init.sql
    healthcheck:
      test: ["CMD-SHELL", "pg_isready -U postgres -d dichvutracuu"]
      interval: 10s
      timeout: 5s
      retries: 5

volumes:
  postgres_data:
